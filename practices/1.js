const camelVar = 0;
const snake_var = 1;
const PascalVar = 2;

// Alert:
// 1. Выводит только строки
// 2. Выводит только одно значение
// 3. Используется для взаимодействия с пользователем
alert([camelVar, snake_var, PascalVar]);

// Console.log:
// 1. Выводит любые типы данных
// 2. Выводит любое количество параметров
// 3. Используется для отладки кода
console.log(camelVar, snake_var, PascalVar);